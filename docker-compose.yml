services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile
    container_name: inventory_dashboard_frontend
    image: resilientbd/inventory-dashboard:0.0.1
    ports:
      - "8080:80"
    restart: always
